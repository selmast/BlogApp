@model List<BlogApp.Models.Category>

<div class="category-strip">
    @foreach (var category in Model)
    {
        <a asp-controller="Post" asp-action="Category" asp-route-id="@category.Id" class="category-strip-link">@category.Name</a>
    }
</div>